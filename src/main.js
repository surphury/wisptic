/* document.addEventListener('readystatechange', () => {
	if (document.readyState === 'complete') {
		new Swiper(".mySwiper", {
			pagination: {
				el: ".swiper-pagination",
				clickable: true,
			},
		});
	}
}, { once: true }); */

/* plan slider */

/* document.querySelector('plans__list')

const $slideToLeft = document.createElement('button');
const $slideToRight = document.createElement('button');

const slide = () => {

} */
/* plan slider */